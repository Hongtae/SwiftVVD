import DKGUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image("Test.png")
            Text("Hello, world!")
            Text("Hello, world!")
        }
    }
}
